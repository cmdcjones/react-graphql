{
  "name": "react-graphql",
  "private": true,
  "version": "0.0.0",
  "description": "A React application using a GraphQL API and Prisma (PostgreSQL) on the backend.",
  "type": "module",
  "scripts": {
  }
}
